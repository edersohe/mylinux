#!/bin/sh

export EDITOR=nvim
export VISUAL=nvim
export TERM=xterm-kitty
export TERMINAL=kitty
export PAGER="nvim -Rn"
export MANPAGER="nvim -Rn -c 'set ft=man'"
export PATH=$HOME/go/bin:$HOME/.local/bin:$PATH
export DMENU_OPTS="-b -i -fn \"NotoSans Nerd Font:size=10\""

if [ -n "$SWAYSOCK" ] && command -v swaymsg >/dev/null; then
    export MSG_PROGRAM='swaymsg'
elif [ -n "$I3SOCK" ] && command -v i3-msg >/dev/null; then
	export MSG_PROGRAM='i3-msg'
fi

eval "$(starship init bash)"

alias ls="exa -G --color always --icons -a -s type"
alias ll="exa -l --color always --icons -a -s type "
alias lg="lazygit"
alias paru="paru --batflags '--color never'"
alias bat="bat -pp --theme='Nord'"
alias cat="bat"
